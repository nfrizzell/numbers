CREATE TABLE count (id VARCHAR(10) UNIQUE NOT NULL PRIMARY KEY, total BIGINT);
CREATE TABLE ratelimit (ipaddr VARCHAR(20) UNIQUE NOT NULL PRIMARY KEY, blacklisted BOOLEAN NOT NULL, last_request BIGINT NOT NULL);